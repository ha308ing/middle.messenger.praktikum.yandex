{{{memberLogoUsername}}}

{{#unless isCurrentUser}}
  {{#unless isAdmin}}
    {{#if isMember}}
      {{{iconButtonClose}}}
    {{else}}
      {{{iconButtonInvite}}}
    {{/if}}
  {{/unless}}
{{/unless}}
